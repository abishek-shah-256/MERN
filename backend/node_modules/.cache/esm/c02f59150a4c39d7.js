let bcrypt;_0a4‍.x([["hashPassword",()=>hashPassword],["compPassword",()=>compPassword]]);_0a4‍.w("bcrypt",[["default",["bcrypt"],function(v){bcrypt=v}]]);
//password lai hash garne

       const hashPassword = (password) =>{
    return new Promise( (resolve,reject)=>{
        bcrypt.genSalt(12,(err,salt)=>{
            if(err) {
                 console.log("Salting error")
                 reject(err);
            }
            bcrypt.hash(password,salt,(err,hash)=>{
                if(err){
                    console.log("password hashing error")
                    reject(err)
                }
                resolve(hash)
            })
        })
    })
}

//compares and return true or false
        const compPassword = (password,hashedPassword)=>{
    return bcrypt.compare(password,hashedPassword)
}