let mongoose;_a9f‍.x([["default",()=>_a9f‍.o]]);_a9f‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const {Schema} = mongoose;

const addressSchema = new Schema({
    address_state:{
        type:String,
        required:true, 
    },
    address_city:{
        type:String,
        required:true,
    }
  });

const userSchema = new Schema({
    username:{
        type:String,
        required:true
    },
    password:{
        type:String,
        required:true,
    },
    email:{
        type:String,
        required:true,
        unique:true

    },
    address:addressSchema

    
},
{
    timestamps:true
})
_a9f‍.d(mongoose.model("users",userSchema));