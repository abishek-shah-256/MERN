let users,hashPassword,compPassword,jwt;_96a‍.x([["SignUp",()=>SignUp],["Login",()=>Login],["Logout",()=>Logout],["Users",()=>Users],["SingleUser",()=>SingleUser],["EditUser",()=>EditUser],["DeleteUser",()=>DeleteUser]]);_96a‍.w("../model/schema",[["default",["users"],function(v){users=v}]]);_96a‍.w("../utils/auth",[["hashPassword",["hashPassword"],function(v){hashPassword=v}],["compPassword",["compPassword"],function(v){compPassword=v}]]);_96a‍.w("jsonwebtoken",[["default",["jwt"],function(v){jwt=v}]]);



       const SignUp = async (req, res) => {
  // console.log(req.body)
  const { username, password, email, address_state, address_city ,weather} = req.body;

  if (!username) return res.status(400).send("Name is required");
  if (!password) return res.status(400).send("Password is required");
  if (!email) return res.status(400).send("Email is required");
  if (!address_state) return res.status(400).send("Address is required");
  if (!address_city) return res.status(400).send("Address is required");
  if (!weather) return res.status(400).send("Temperature is required");

  let userExist = await users
    .findOne({
      username,
      email
    })
    .exec();
  if (userExist)
    return res.status(400).send("Username or email already exists");

  const hashedPassword = await hashPassword(password);
  // console.log(hashedPassword)

  const userData = new users({
    username,
    password: hashedPassword,
    email,
    address: {
      address_state,
      address_city,
      weather
    },
  });

  await userData.save();
  return res.json({ Message: "User has been registered" });
};

//loginnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnnn
       const Login = async (req, res) => {
  const { username, password } = req.body;
  try {
    const findUser = await users
      .findOne({
        username,
      })
      .exec();

    if (!findUser) return res.status(404).send("User xaina db ma!!");
    if (findUser) {
      const isMatch = await compPassword(password, findUser.password);

      if (!isMatch) return res.status(401).send("password not matched");

      const token = jwt.sign(
        {
          userId: findUser._id,
          username: findUser.username,
        },

        process.env.LOGIN_SECRET,
        {
          expiresIn: "7d",
        }
      );
      _96a‍.g.console.log(token);

      findUser.password = undefined;
      res.cookie("token", token, {
        //secure:true //jaba sama hhtps request bta aaudaina taba sama token save hudaina browser ma in production
        httpOnly: true
      });
      res.json(findUser);
    }

  } 
  //try block end
  catch (err) {
    _96a‍.g.console.log(err);
    console.log("error aayo");
  }
};
//logoutttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttttt
       const Logout=async(req,res)=>{
  res.clearCookie('token')
  return res.json({Message : "Bye Bye"})
}
//display user tableeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeeee
       const Users = async (req, res) => {
  const userInfo = await users
    .find({
      //empty codda sabai aauxa
    })
    .select("-password")
    .exec();
  //console.log(userInfo)
  res.json(userInfo);
};

//Getting data from db for editing user-->
       const SingleUser = async (req, res) => {
  const { id } = req.body;
  let findSingleUser = await users
    .findById(id) //.select('-password')
    .exec();
  res.json(findSingleUser);
  _96a‍.g.console.log(findSingleUser);
};
//editing user---->
       const EditUser = async (req, res) => {
  _96a‍.g.console.log("backma data aako" + req.body.id);
  const { id, username, password, email, address_state, address_city ,weather} =
    req.body;

  const updateUser = await users
    .findByIdAndUpdate(id, {
      username,
      password,
      email,
      address: { address_state, address_city },
    })
    .exec();
  res.status(200).send("user updated successfully");
};

       const DeleteUser = async (req, res) => {
  _96a‍.g.console.log(req.query);
  const { userid } = req.query;

  const deleteUser = await users.findByIdAndDelete(userid).exec();
  res.status(200).send("User deleted successfullly");
};
